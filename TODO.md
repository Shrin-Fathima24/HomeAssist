# Flutter Compilation Fix TODO

## Steps to complete:
1. [x] Update HomeAssist/lib/main.dart: Add missing imports and remove `const` from all route builders\n2. [x] Update HomeAssist/lib/login.dart: Remove `const` from AdminWorkersScreen in Navigator.push
3. [ ] Test: Run `flutter run` (select Chrome) to verify compilation succeeds
4. [ ] [Complete]

